#include <stdio.h>

int main() {
	    char c;
	        for (c = '0'; c <= '9'; ++c) {
			        putchar(c);
				    }
		    char cr
			        for (cr = 'a'; cr <= 'f'; ++cr) {
					        putchar(cr);
						    }

		        putchar('\n');

			    return 0;
}

