#include <stdio.h>

int main() {
	    char c;
	        char cr;
		    for (c = '0'; c <= '9'; ++c) {
			            putchar(c);
				        }
		        for (cr = 'a'; cr <= 'f'; ++cr) {
				        putchar(cr);
					    }

			    putchar('\n');

			        return 0;
}

